apiVersion: v1
kind: Service
metadata:
  name: aegis-api
  namespace: aegis
  labels:
    app: aegis-api
spec:
  type: ClusterIP
  selector:
    app: aegis-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
